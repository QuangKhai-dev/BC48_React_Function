import React from 'react';

const DemoProps = ({ number, hoTen }) => {
  // console.log(props.number);
  console.log(number);
  console.log(hoTen);
  return <div>DemoProps</div>;
};

export default DemoProps;
